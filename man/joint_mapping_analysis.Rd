% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_mapping_analysis.R
\name{joint_mapping_analysis}
\alias{joint_mapping_analysis}
\title{Joint Mapping Analysis for Multiple Traits}
\usage{
joint_mapping_analysis(
  vecPheno,
  vecH2,
  PhenoData,
  method,
  vecThrVal,
  GenoData_EST,
  GenoData_QTL = NULL
)
}
\arguments{
\item{vecPheno}{Character vector specifying the trait names to analyze.}

\item{vecH2}{Numeric vector containing heritability values for each trait.
Must have the same length as \code{vecPheno}.}

\item{PhenoData}{Data frame containing phenotype data with columns:
\itemize{
  \item \code{Indi}: Individual identifiers
  \item \code{Popu}: Population identifiers
  \item \code{MA}: Maternal allele information
  \item \code{PA}: Paternal allele information
  \item Additional columns: Trait measurements
}}

\item{method}{Character string specifying the mapping method:
\itemize{
  \item \code{"AM"}: Association Mapping - uses P-values for significance testing
  \item \code{"LM"}: Linkage Mapping - uses LOD scores for significance testing
}}

\item{vecThrVal}{Numeric vector containing significance thresholds from permutation tests.
For AM method: P-value thresholds for each trait.
For LM method: P-value and LOD score thresholds for each trait.}

\item{GenoData_EST}{Data frame containing genotype data for cofactor selection.}

\item{GenoData_QTL}{Optional data frame containing genotype data for QTL scanning.
Required when \code{method="LM"}, ignored when \code{method="AM"}.}
}
\value{
A list containing comprehensive QTL analysis results for each trait:
  \itemize{
    \item \code{marker}: Marker name
    \item \code{chr}: Chromosome number
    \item \code{pos}: Marker position (in centimorgans)
    \item \code{LOD}: LOD score (LM method) or P-value (AM method)
    \item \code{Coe_Pop1, Coe_Pop2, ...}: QTL effects for each population
    \item \code{PartialR2}: Partial R-squared for each QTL
    \item \code{pG}: Percentage of genetic variance explained
  }
}
\description{
Performs comprehensive joint mapping analysis for multiple traits using either Association Mapping (AM) 
or Linkage Mapping (LM) methods to identify QTL affecting multiple traits simultaneously. This function
implements sophisticated statistical methods for multivariate QTL analysis with comprehensive
cofactor selection and significance testing.
}
\details{
This function implements a comprehensive joint mapping analysis framework:

\strong{Analysis Pipeline:}
\enumerate{
  \item \strong{Data Validation}: Checks input data consistency and structure
  \item \strong{Cofactor Selection}: Performs stepwise regression for each trait separately
  \item \strong{QTL Scanning}: Conducts genome-wide scanning using multivariate models
  \item \strong{Significance Testing}: Identifies significant QTL based on permutation thresholds
  \item \strong{Effect Estimation}: Calculates QTL effects and variance components
  \item \strong{Model Assessment}: Computes partial R-squared and genetic variance explained
}

\strong{Association Mapping (AM):}
\itemize{
  \item Uses P-values for significance testing
  \item Requires only \code{GenoData_EST}
  \item Applies Bonferroni correction for multiple testing
  \item Suitable for association studies with dense marker coverage
}

\strong{Linkage Mapping (LM):}
\itemize{
  \item Uses LOD scores for significance testing
  \item Requires both \code{GenoData_EST} and \code{GenoData_QTL}
  \item Supports virtual marker interpolation
  \item Suitable for family-based studies with genetic maps
}

\strong{Statistical Methods:}
\itemize{
  \item \strong{Stepwise Regression}: Uses StepReg package for cofactor selection
  \item \strong{Multivariate Models}: Handles multiple traits simultaneously
  \item \strong{Model Comparison}: Compares reduced and full models for significance
  \item \strong{Effect Estimation}: Calculates QTL effects and standard errors
  \item \strong{Variance Components}: Computes partial and total genetic variance explained
}

\strong{Population Handling:}
\itemize{
  \item \strong{Single Population}: Simple linear models without population effects
  \item \strong{Multiple Populations}: Models include population and interaction effects
  \item \strong{Population Structure}: Accounts for population structure in the analysis
}

\strong{Output Interpretation:}
\itemize{
  \item \strong{QTL Effects}: Magnitude and direction of QTL effects on traits
  \item \strong{Partial R-squared}: Proportion of variance explained by each QTL
  \item \strong{Genetic Variance}: Percentage of genetic variance explained relative to heritability
  \item \strong{Significance}: Statistical significance based on permutation thresholds
}
}
\examples{
# Example phenotype data
pheno_data <- data.frame(
  Indi = paste0("Ind", 1:100),
  Popu = rep(c("Pop1", "Pop2"), each = 50),
  MA = rep(1:2, 50),
  PA = rep(1:2, 50),
  Trait1 = rnorm(100, mean = 100, sd = 15),
  Trait2 = rnorm(100, mean = 50, sd = 8)
)

# Example genotype data
geno_est <- matrix(sample(c(0,1,2), 100*50, replace = TRUE), 
                   nrow = 100, ncol = 50)
colnames(geno_est) <- paste0("M", 1:50)

# Heritability values
h2 <- c(0.3, 0.4)

# Threshold values from permutation test
thresholds_am <- c(0.05, 0.05)  # P-value thresholds for AM
thresholds_lm <- c(0.05, 0.05, 2.5, 3.0)  # P-value and LOD thresholds for LM

# Joint mapping analysis - Association mapping
results_am <- joint_mapping_analysis(c("Trait1", "Trait2"), h2, pheno_data, method = "AM",
                 thresholds_am, geno_est)

# View results for first trait
print(results_am$Trait1)

# Joint mapping analysis - Linkage mapping
geno_qtl <- matrix(sample(c(0,1,2), 100*30, replace = TRUE), 
                   nrow = 100, ncol = 30)
colnames(geno_qtl) <- paste0("Q", 1:30)

results_lm <- joint_mapping_analysis(c("Trait1", "Trait2"), h2, pheno_data, method = "LM",
                 thresholds_lm, geno_est, geno_qtl)

# View results for second trait
print(results_lm$Trait2)

# Example with three traits
pheno_data_3t <- cbind(pheno_data, Trait3 = rnorm(100, mean = 25, sd = 5))
h2_3t <- c(0.3, 0.4, 0.25)
thresholds_3t <- c(0.05, 0.05, 0.05, 2.5, 3.0, 2.8)

results_3t <- joint_mapping_analysis(c("Trait1", "Trait2", "Trait3"), h2_3t, pheno_data_3t, 
                 method = "AM", thresholds_3t[1:3], geno_est)

}
\references{
Jiang, C. and Zeng, Z.B. (1995). Multiple trait analysis of genetic mapping for 
quantitative trait loci. Genetics, 140(3), 1111-1127.
}
\seealso{
\code{\link{permutation_test_joint_mapping}} for permutation test thresholds,
         \code{\link{linkage_pleiotropy_analysis}} for linkage-pleiotropy analysis,
         \code{\link{calculate_genotype_probabilities}} for genotype probability calculations
}
